{
  "scripts": {
    "develop": "concurrently \"cd server && node --watch server\" \"cd client && npm run start\"",
    "dev": "concurrently \"cd server && node --watch server\" \"cd client && npm run start\""
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "react-router-dom": "^6.11.1"
  }
}
